@model PagedList.IPagedList<Models.Framework.Tag>
@using PagedList.Mvc
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@section scripts{
    <script src="~/Asset/Asset Admin/js/controller/IndexTagModel.js"></script>
}
<div class="table-responsive">
    <table class="table table-striped table-bordered table-hover" id="dataTables-example">

        <thead>
            <tr>
                <th>ID</th>
                <th>#</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr class="odd gradeX" id="row_@item.ID">
                    <td>@item.ID</td>
                    <td>
                        <a href="/Admin/Tag/Edit/@item.ID">Sửa</a> |
                        <a href="#" class="btn_delete" data-id=@item.ID>Delete</a>
                    </td>
                    <td>@item.Name</td>
                </tr>
            }



        </tbody>
    </table>
    <a href="/Admin/Tag/Create" class="btn btn-default">Create New</a>
    @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
</div>
<!-- /.table-responsive -->
